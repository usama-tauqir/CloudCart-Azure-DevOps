name: CloudCart CI/CD

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  security-events: write

env:
  RESOURCE_GROUP: ${{ vars.AZURE_RESOURCE_GROUP }}
  LOCATION: ${{ vars.AZURE_LOCATION }}
  ACR_NAME: ${{ vars.ACR_NAME }}
  ACA_ENVIRONMENT: ${{ vars.ACA_ENVIRONMENT }}


# =========================================================
# 1. CI - Dependencies, Tests, Compilation
# =========================================================

jobs:

  ci:
    name: Build and Test
    runs-on: ubuntu-latest

    steps:

      # -----------------------------------------------------
      # Checkout
      # -----------------------------------------------------

      - name: Checkout repository
        uses: actions/checkout@v4

      # -----------------------------------------------------
      # Locate eShop solution automatically
      # -----------------------------------------------------

      - name: Find eShop solution
        shell: bash
        run: |
          echo "Repository root:"
          pwd

          echo "Searching for eShop.slnx..."

          SOLUTION_PATH=$(find "$GITHUB_WORKSPACE" \
            -type f \
            -name "eShop.slnx" \
            -print \
            -quit)

          if [ -z "$SOLUTION_PATH" ]; then
            echo "ERROR: eShop.slnx was not found."

            echo "Repository structure:"
            find "$GITHUB_WORKSPACE" -maxdepth 4 -type d

            exit 1
          fi

          APP_ROOT=$(dirname "$SOLUTION_PATH")

          echo "Solution found:"
          echo "$SOLUTION_PATH"

          echo "Application root:"
          echo "$APP_ROOT"

          echo "APP_ROOT=$APP_ROOT" >> "$GITHUB_ENV"

      # -----------------------------------------------------
      # .NET SDK
      # -----------------------------------------------------

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Show .NET information
        run: |
          dotnet --version
          dotnet --info

      # -----------------------------------------------------
      # Dependency Installation
      # -----------------------------------------------------

      - name: Restore dependencies
        working-directory: ${{ env.APP_ROOT }}
        run: |
          dotnet restore eShop.slnx -p:TargetFramework=net10.0 -p:TargetFramework=net10.0

      # -----------------------------------------------------
      # Dependency Security Scan
      # -----------------------------------------------------

      - name: Scan NuGet dependencies
        working-directory: ${{ env.APP_ROOT }}
        run: |
          dotnet list eShop.slnx package \
            --framework net10.0 \
            --vulnerable \
            --include-transitive \
            | tee dependency-audit.txt

      # -----------------------------------------------------
      # Automated Unit Testing
      # -----------------------------------------------------

      - name: Run tests
        working-directory: ${{ env.APP_ROOT }}
        run: |
          dotnet test eShop.slnx \
            --framework net10.0 \
            --configuration Release \
            --no-restore \
            --logger "trx" \
            --results-directory TestResults

      # -----------------------------------------------------
      # Code Compilation
      # -----------------------------------------------------

      - name: Build solution
        working-directory: ${{ env.APP_ROOT }}
        run: |
          dotnet build eShop.slnx \
            --framework net10.0 \
            --configuration Release \
            --no-restore

      # -----------------------------------------------------
      # Test Artifacts
      # -----------------------------------------------------

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cloudcart-test-results
          path: ${{ env.APP_ROOT }}/TestResults
          if-no-files-found: ignore

      - name: Upload dependency audit
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dependency-audit
          path: ${{ env.APP_ROOT }}/dependency-audit.txt
          if-no-files-found: ignore


  # =========================================================
  # 2. CODE SECURITY SCANNING
  # =========================================================

  codeql:
    name: CodeQL Security Scan
    runs-on: ubuntu-latest

    steps:

      # -----------------------------------------------------
      # Checkout
      # -----------------------------------------------------

      - name: Checkout repository
        uses: actions/checkout@v4

      # -----------------------------------------------------
      # Locate eShop solution automatically
      # -----------------------------------------------------

      - name: Find eShop solution
        shell: bash
        run: |
          echo "Searching for eShop.slnx..."

          SOLUTION_PATH=$(find "$GITHUB_WORKSPACE" \
            -type f \
            -name "eShop.slnx" \
            -print \
            -quit)

          if [ -z "$SOLUTION_PATH" ]; then
            echo "ERROR: eShop.slnx was not found."
            exit 1
          fi

          APP_ROOT=$(dirname "$SOLUTION_PATH")

          echo "Solution found:"
          echo "$SOLUTION_PATH"

          echo "Application root:"
          echo "$APP_ROOT"

          echo "APP_ROOT=$APP_ROOT" >> "$GITHUB_ENV"

      # -----------------------------------------------------
      # .NET SDK
      # -----------------------------------------------------

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      # -----------------------------------------------------
      # Initialize CodeQL BEFORE build
      # -----------------------------------------------------

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: csharp

      # -----------------------------------------------------
      # Restore NuGet packages
      # -----------------------------------------------------

      - name: Restore dependencies for CodeQL
        working-directory: ${{ env.APP_ROOT }}
        run: |
          dotnet restore eShop.slnx

      # -----------------------------------------------------
      # Build application for CodeQL
      # -----------------------------------------------------

      - name: Build for CodeQL
        working-directory: ${{ env.APP_ROOT }}
        run: |
          dotnet build eShop.slnx \
            --framework net10.0 \
            --configuration Release \
            --no-restore

      # -----------------------------------------------------
      # Analyze
      # -----------------------------------------------------

      - name: Analyze source code
        uses: github/codeql-action/analyze@v3


  # =========================================================
  # 3. INFRASTRUCTURE PROVISIONING
  # Only runs after CI + CodeQL pass
  # Only deploys from main branch
  # =========================================================

  infrastructure:
    name: Provision Azure Infrastructure

    if: >
      github.event_name == 'push' &&
      github.ref == 'refs/heads/main'

    needs:
      - ci
      - codeql

    runs-on: ubuntu-latest

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

      # -----------------------------------------------------
      # Azure OIDC Authentication
      # -----------------------------------------------------

      - name: Azure Login
        uses: azure/login@v3
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      # -----------------------------------------------------
      # Register Azure Resource Providers
      # -----------------------------------------------------

      - name: Register Azure providers
        run: |
          az provider register \
            --namespace Microsoft.ContainerRegistry

          az provider register \
            --namespace Microsoft.App

          az provider register \
            --namespace Microsoft.OperationalInsights

      # -----------------------------------------------------
      # Resource Group
      # -----------------------------------------------------

      - name: Create Resource Group if missing
        run: |
          az group create \
            --name "$RESOURCE_GROUP" \
            --location "$LOCATION"

      # -----------------------------------------------------
      # Azure Container Registry
      # -----------------------------------------------------

      - name: Provision Azure Container Registry
        shell: bash
        run: |
          if az acr show \
              --name "$ACR_NAME" \
              --resource-group "$RESOURCE_GROUP" \
              >/dev/null 2>&1
          then
              echo "ACR already exists."
          else
              echo "Creating ACR..."

              az acr create \
                --resource-group "$RESOURCE_GROUP" \
                --name "$ACR_NAME" \
                --sku Basic
          fi

      # -----------------------------------------------------
      # Azure Container Apps CLI extension
      # -----------------------------------------------------

      - name: Install Container Apps extension
        run: |
          az extension add \
            --name containerapp \
            --upgrade

      # -----------------------------------------------------
      # Container Apps Environment
      # -----------------------------------------------------

      - name: Provision Container Apps Environment
        shell: bash
        run: |
          if az containerapp env show \
              --name "$ACA_ENVIRONMENT" \
              --resource-group "$RESOURCE_GROUP" \
              >/dev/null 2>&1
          then
              echo "Container Apps environment already exists."
          else
              echo "Creating Container Apps environment..."

              az containerapp env create \
                --name "$ACA_ENVIRONMENT" \
                --resource-group "$RESOURCE_GROUP" \
                --location "$LOCATION"
          fi


  # =========================================================
  # 4. CONTAINERIZATION + REGISTRY UPLOAD
  # =========================================================

  docker:
    name: Docker - ${{ matrix.app }}

    if: >
      github.event_name == 'push' &&
      github.ref == 'refs/heads/main'

    needs:
      - infrastructure

    runs-on: ubuntu-latest

    strategy:
      fail-fast: false

      matrix:
        include:

          - app: catalog-api
            image: eshop-catalog-api
            dockerfile: src/Catalog.API/Dockerfile

          - app: basket-api
            image: eshop-basket-api
            dockerfile: src/Basket.API/Dockerfile

          - app: ordering-api
            image: eshop-ordering-api
            dockerfile: src/Ordering.API/Dockerfile

          - app: identity-api
            image: eshop-identity-api
            dockerfile: src/Identity.API/Dockerfile

          - app: webhooks-api
            image: eshop-webhooks-api
            dockerfile: src/Webhooks.API/Dockerfile

          - app: order-processor
            image: eshop-order-processor
            dockerfile: src/OrderProcessor/Dockerfile

          - app: payment-processor
            image: eshop-payment-processor
            dockerfile: src/PaymentProcessor/Dockerfile

          - app: webapp
            image: eshop-webapp
            dockerfile: src/WebApp/Dockerfile

          - app: webhook-client
            image: eshop-webhook-client
            dockerfile: src/WebhookClient/Dockerfile

    steps:

      # -----------------------------------------------------
      # Checkout
      # -----------------------------------------------------

      - name: Checkout repository
        uses: actions/checkout@v4

      # -----------------------------------------------------
      # Locate application root
      # -----------------------------------------------------

      - name: Find eShop solution
        shell: bash
        run: |
          SOLUTION_PATH=$(find "$GITHUB_WORKSPACE" \
            -type f \
            -name "eShop.slnx" \
            -print \
            -quit)

          if [ -z "$SOLUTION_PATH" ]; then
            echo "ERROR: eShop.slnx was not found."
            exit 1
          fi

          APP_ROOT=$(dirname "$SOLUTION_PATH")

          echo "Solution:"
          echo "$SOLUTION_PATH"

          echo "Application root:"
          echo "$APP_ROOT"

          echo "APP_ROOT=$APP_ROOT" >> "$GITHUB_ENV"

      # -----------------------------------------------------
      # Azure Authentication
      # -----------------------------------------------------

      - name: Azure Login
        uses: azure/login@v3
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      # -----------------------------------------------------
      # Registry Authentication
      # -----------------------------------------------------

      - name: Login to Azure Container Registry
        run: |
          az acr login --name "$ACR_NAME"

      # -----------------------------------------------------
      # Image Name
      # -----------------------------------------------------

      - name: Prepare image name
        run: |
          echo \
          "IMAGE=${ACR_NAME}.azurecr.io/${{ matrix.image }}:${GITHUB_SHA}" \
          >> "$GITHUB_ENV"

      # -----------------------------------------------------
      # Docker Build
      # -----------------------------------------------------

      - name: Build Docker image
        working-directory: ${{ env.APP_ROOT }}
        run: |
          echo "Building application: ${{ matrix.app }}"
          echo "Dockerfile: ${{ matrix.dockerfile }}"
          echo "Image: $IMAGE"

          docker build \
            -f "${{ matrix.dockerfile }}" \
            -t "$IMAGE" \
            .

      # -----------------------------------------------------
      # Container Security Scan
      # -----------------------------------------------------

      - name: Scan Docker image with Trivy
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ${{ env.IMAGE }}
          format: table
          exit-code: "1"
          ignore-unfixed: true
          severity: "CRITICAL,HIGH"

      # -----------------------------------------------------
      # Push Image → ACR
      # -----------------------------------------------------

      - name: Push image to Azure Container Registry
        run: |
          docker push "$IMAGE"


  # =========================================================
  # 5. SERVER DEPLOYMENT
  # Azure Container Apps
  # =========================================================

  deploy:
    name: Deploy - ${{ matrix.app }}

    needs:
      - docker

    runs-on: ubuntu-latest

    strategy:
      fail-fast: false

      matrix:
        include:

          - app: catalog-api
            image: eshop-catalog-api
            ingress: internal

          - app: basket-api
            image: eshop-basket-api
            ingress: internal

          - app: ordering-api
            image: eshop-ordering-api
            ingress: internal

          - app: identity-api
            image: eshop-identity-api
            ingress: external

          - app: webhooks-api
            image: eshop-webhooks-api
            ingress: internal

          - app: order-processor
            image: eshop-order-processor
            ingress: disabled

          - app: payment-processor
            image: eshop-payment-processor
            ingress: disabled

          - app: webapp
            image: eshop-webapp
            ingress: external

          - app: webhook-client
            image: eshop-webhook-client
            ingress: external

    steps:

      # -----------------------------------------------------
      # Azure Authentication
      # -----------------------------------------------------

      - name: Azure Login
        uses: azure/login@v3
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      # -----------------------------------------------------
      # Deploy to Azure Container Apps
      # -----------------------------------------------------

      - name: Deploy Container App
        uses: azure/container-apps-deploy-action@v2
        with:
          acrName: ${{ vars.ACR_NAME }}

          containerAppName: ${{ matrix.app }}

          containerAppEnvironment: ${{ vars.ACA_ENVIRONMENT }}

          resourceGroup: ${{ vars.AZURE_RESOURCE_GROUP }}

          imageToDeploy: >
            ${{ vars.ACR_NAME }}.azurecr.io/${{ matrix.image }}:${{ github.sha }}

          ingress: ${{ matrix.ingress }}

          targetPort: 8080


  # =========================================================
  # 6. PRODUCTION SMOKE TEST
  # =========================================================

  smoke-test:
    name: Production Smoke Test

    needs:
      - deploy

    runs-on: ubuntu-latest

    steps:

      # -----------------------------------------------------
      # Azure Authentication
      # -----------------------------------------------------

      - name: Azure Login
        uses: azure/login@v3
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      # -----------------------------------------------------
      # Obtain WebApp URL
      # -----------------------------------------------------

      - name: Get WebApp URL
        run: |
          WEBAPP_FQDN=$(az containerapp show \
            --name webapp \
            --resource-group "$RESOURCE_GROUP" \
            --query properties.configuration.ingress.fqdn \
            --output tsv)

          if [ -z "$WEBAPP_FQDN" ]; then
            echo "ERROR: WebApp FQDN could not be determined."
            exit 1
          fi

          echo "WEBAPP_URL=https://${WEBAPP_FQDN}" >> "$GITHUB_ENV"

          echo "Testing:"
          echo "https://${WEBAPP_FQDN}"

      # -----------------------------------------------------
      # Smoke Test
      # -----------------------------------------------------

      - name: Smoke test WebApp
        run: |
          for i in {1..12}
          do

            STATUS=$(curl \
              --silent \
              --output /dev/null \
              --write-out "%{http_code}" \
              "$WEBAPP_URL" || true)

            echo "Attempt $i → HTTP $STATUS"

            if [ "$STATUS" = "200" ] ||
               [ "$STATUS" = "302" ]
            then
              echo "Smoke test passed."
              exit 0
            fi

            sleep 10

          done

          echo "Smoke test failed."
          exit 1


  # =========================================================
  # 7. SLACK SUCCESS NOTIFICATION
  # =========================================================

  slack-success:
    name: Slack Notification

    needs:
      - smoke-test

    if: success()

    runs-on: ubuntu-latest

    steps:

      - name: Notify Slack
        uses: slackapi/slack-github-action@v4.0.0
        with:

          webhook: ${{ secrets.SLACK_WEBHOOK_URL }}

          webhook-type: incoming-webhook

          payload: |
            {
              "text": "✅ CloudCart deployment successful!\nRepository: ${{ github.repository }}\nBranch: ${{ github.ref_name }}\nCommit: ${{ github.sha }}\nTriggered by: ${{ github.actor }}\nWorkflow: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
            }
